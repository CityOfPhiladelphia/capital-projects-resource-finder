import { test, expect } from '@playwright/experimental-ct-vue';
import ExpandColapseContent from '../../src/components/ExpandColapseContent.vue';


test.describe("ExpandColapseContent: Component Rendering Tests", () => {

  test(`ExpandColapseContent`, async ({ mount }) => {
    // const expandColapseContent = await mount(ExpandColapseContent, { props: testProps });

  })

})
